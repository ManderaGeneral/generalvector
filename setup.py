
from setuptools import setup, find_packages

setup(
    name = "generalvector",
    version = "1.0.0",
    description = (""
                   "Simple vectors."
                   ""),
    packages = find_packages(),
    install_requires = ['wheel']
)


