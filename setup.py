
from setuptools import setup, find_packages

setup(
    name = "generalvector",
    version = "1.1.0",
    description = (""
                   "Added 3-d vectors. "
                   "Simple vectors."
                   ""),
    packages = find_packages(),
    install_requires = ['wheel']
)


